The Polar Sandbox payment has now ACTUALLY been completed successfully.

Polar Sandbox dashboard currently shows:

- Revenue: $5
- Orders: 1
- Checkout conversion: 50%
- Order Paid: $5
- Customer Created: Sandbox Verification

Do NOT create another payment.

Now perform a READ-ONLY end-to-end verification of the payment that was just completed.

I need you to trace the exact transaction from Polar → webhook → UpnDownBid → payment record → ledger → campaign/Boost/ranking.

Do not modify application code unless you discover an actual defect.

Verify the following:

1. POLAR WEBHOOK

Find the webhook event generated by the completed $5 sandbox order.

Confirm:
- event ID
- event type
- delivery status
- delivery timestamp
- webhook endpoint used
- signature verification result

Confirm that it was delivered to:

POST /api/payments/webhooks/polar

2. APPLICATION LOGS

Find the corresponding webhook request in the UpnDownBid API logs.

Confirm:
- webhook was received
- signature was verified
- event was accepted
- no 4xx/5xx response occurred
- no duplicate processing occurred

Do NOT print any secret, access token, webhook secret, customer email, card data, or other sensitive information.

3. INTERNAL PAYMENT

Find the exact internal payment created for this checkout.

Report:

- internal payment ID
- Polar checkout/order ID
- campaign ID
- action type (Claim / Boost / Push Down)
- amount
- currency
- state before webhook
- state after webhook

Expected:

pending → paid/succeeded

4. LEDGER

Check the immutable payment/ledger records.

Record:

ledger entries BEFORE
ledger entries AFTER

Verify that exactly ONE successful payment ledger entry was created for this transaction.

5. CAMPAIGN / BOOST

Verify the actual campaign state.

Report:

- Boost/claim count before payment
- Boost/claim count after payment
- ranking power before payment
- ranking power after payment

Verify that the action was applied exactly once.

6. IDEMPOTENCY

Using the SAME Polar event ID, perform a safe idempotency/replay verification if the existing test tooling allows it.

DO NOT create another real payment.

Verify that replaying the same event produces:

- no second ledger entry
- no second Boost
- no second ranking change
- no duplicate payment
- successful/idempotent response

If replaying the actual webhook would be unsafe, use the existing isolated webhook replay test mechanism instead and clearly state that.

7. RETURN FLOW

Check the browser return associated with this payment.

Verify that the application eventually displayed:

payment confirmed/success

and did NOT display success before the server verified the webhook.

8. DATABASE CONSISTENCY

Verify:

Polar order = 1
internal payment = 1
ledger success entry = 1
campaign action = 1

There must be no orphan payment, duplicate ledger entry, or duplicate campaign action.

9. FINAL VERDICT

Give me a strict result:

POLAR SANDBOX PAYMENT: PASS/FAIL
WEBHOOK DELIVERY: PASS/FAIL
SIGNATURE VERIFICATION: PASS/FAIL
PAYMENT STATE UPDATE: PASS/FAIL
LEDGER UPDATE: PASS/FAIL
CAMPAIGN ACTION: PASS/FAIL
IDEMPOTENCY: PASS/FAIL
RETURN FLOW: PASS/FAIL
DATABASE CONSISTENCY: PASS/FAIL

Then give a concise transaction chain:

Polar $5 payment
↓
Polar Order
↓
Polar Webhook
↓
UpnDownBid webhook verification
↓
Internal Payment
↓
Ledger
↓
Campaign/Boost
↓
Leaderboard

Only call the integration END-TO-END VERIFIED if every step above passes.

If anything fails, DO NOT silently fix it. Tell me exactly which step failed and why.